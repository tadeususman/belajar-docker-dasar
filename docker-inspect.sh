# untuk melihat detail informasi dari docker image, container, volume, network

docker image inspect [nama image]

docker container inspect [nama contaner]

docker volume inspect [nama volume]

docker network inspect [nama network]